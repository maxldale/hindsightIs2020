import 'ts-node/register'; // Add this to import TypeScript files
import { ExpoConfig } from 'expo/config';

const config: ExpoConfig = {
  name: 'Hindsight is 20/20',
  slug: 'HindsightIs2020',
};

export default config;
